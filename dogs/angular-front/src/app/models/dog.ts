export class Dog {
  id: string;
  name: string;
  area: string;
  image: string;

  constructor(id: string, name: string, area: string, image: string){
    this.id = id;
    this.name = name;
    this.area = area;
    this.image = image;
  };
}